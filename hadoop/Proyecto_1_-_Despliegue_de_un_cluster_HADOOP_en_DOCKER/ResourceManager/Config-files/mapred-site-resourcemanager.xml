<?xml version="1.0"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<!--
  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License. See accompanying LICENSE file.
-->

<!-- Put site-specific property overrides in this file. -->

<configuration>
  <property>
    <!-- El framework de ejecución (runtime framework) para ejecutar trabajos MapReduce -->
    <name>mapreduce.framework.name</name>
    <value>yarn</value>
    <final>true</final>
  </property>

  <property>
    <!-- Variables de entorno añadidas por el usuario para el proceso MR App Master.
         En este caso, especifica la ubicación de las librerías para el MR App Master-->.
    <name>yarn.app.mapreduce.am.env</name>
    <value>HADOOP_MAPRED_HOME=${HADOOP_HOME}</value>
  </property>
  <property>
    <name>mapreduce.map.env</name>
    <value>HADOOP_MAPRED_HOME=${HADOOP_HOME}</value>
  </property>
  <property>
    <name>mapreduce.reduce.env</name>
    <value>HADOOP_MAPRED_HOME=${HADOOP_HOME}</value>
  </property>

 <!-- Configuración de MR ApplicationMaster -->

  <property>
    <!-- El número de núcleos virtuales de CPU (virtual CPU cores) que necesita el MR AppMaster (valor por defecto 1) -->
    <name>yarn.app.mapreduce.am.resource.cpu-vcores</name>
    <value>1</value>
    <final>true</final>
  </property>

  <property>
    <!-- La cantidad de memoria que necesita el MR AppMaster (valor por defecto 1536) -->
    <name>yarn.app.mapreduce.am.resource.mb</name>
    <value>1024</value>
    <final>true</final>
  </property>

<!-- Configuración de maps y reduces -->

  <property>
    <!-- La relación entre el heap-size y el tamaño del contenedor --> 
    <name>mapreduce.job.heap.memory-mb.ratio</name>
    <value>0.8</value>
    <final>true</final>
  </property>

  <!-- Maps -->
  <property>
    <!-- Variables de entorno añadidas por el usuario para los Maps. 
         En este caso, especifica la ubicación de las librerías para los Maps -->
    <name>mapreduce.map.env</name>
    <value>HADOOP_MAPRED_HOME=${HADOOP_HOME}</value>
  </property>

  <property>
    <!-- El número de núcleos virtuales (virutal cores) a solicitar para cada tarea map (valor por defecto 1) -->
    <name>mapreduce.map.cpu.vcores</name>
    <value>1</value>
    <final>true</final>
  </property>

  <property>
    <!-- Opciones de Java para las JVM Maps -->
    <name>mapreduce.map.java.opts</name>
    <value>-Xmx1024M</value> <!-- Especifica el tamaño máximo, en bytes, de la memoria Java heap -->
    <final>true</final>
  </property>

  <property>
    <!-- La cantidad de memoria a solicitar para cada tarea map.
         Si no se especifica o no es positivo, se calcula como 
         (heapSize / mapreduce.heap.memory-mb.ratio) -->
    <name>mapreduce.map.memory.mb</name>
    <value>-1</value>
    <final>true</final>
  </property>

  <!-- Reduces -->
  <property>
    <!-- Variables de entorno añadidas por el usuario para los Reduces.
         En este caso, especifica la ubicación de las librerías para los Reduces -->
    <name>mapreduce.reduce.env</name>
    <value>HADOOP_MAPRED_HOME=${HADOOP_HOME}</value>
  </property>

  <property>
    <!-- El número de núcleos virtuales (virutal cores) a solicitar para cada tarea de reducción (valor por defecto 1) -->
    <name>mapreduce.reduce.cpu.vcores</name>
    <value>1</value>
    <final>true</final>
  </property>

  <property>
    <!-- Opciones Java para las JVMs Reduces -->
    <name>mapreduce.reduce.java.opts</name>
    <value>-Xmx1024M</value> <!-- Xmx define el tamaño máximo de la pila de Java -->
    <final>true</final>
  </property>

  <property>
    <!-- La cantidad de memoria a solicitar para cada tarea map.
         Si no se especifica o no es positivo, se calcula como 
         (heapSize / mapreduce.heap.memory-mb.ratio -->
    <name>mapreduce.reduce.memory.mb</name>
    <value>-1</value>
    <final>true</final>
  </property>
</configuration>

